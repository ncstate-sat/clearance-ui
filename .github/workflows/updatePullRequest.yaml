name: Update Pull Request

on:
  pull_request:
    types: 
      - opened

jobs:
  update_pr:
    runs-on: sat-hosted
    steps:
      - name: Get SAT Actions
        uses: actions/checkout@v3
        with:
          repository: SAT/sat-actions
          path: sat-actions

      - name: Update Pull Request
        uses: ./sat-actions/update-pr
